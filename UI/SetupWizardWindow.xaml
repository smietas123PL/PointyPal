<Window x:Class="PointyPal.UI.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PointyPal Setup Wizard" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F7"
        FontFamily="Segoe UI Semibold"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">
    <Window.Resources>
        <DropShadowEffect x:Key="PremiumShadow" BlurRadius="15" Color="#000000" Opacity="0.1" ShadowDepth="2"/>
        
        <Style x:Key="WizardButton" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Foreground" Value="#1D1D1F"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F2F2F7"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryWizardButton" TargetType="Button" BasedOn="{StaticResource WizardButton}">
            <Setter Property="Background" Value="#0071E3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0077ED"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1D1D1F"/>
        </Style>

        <Style x:Key="Header1" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="SubHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#86868B"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="12" Background="#F5F5F7" Margin="10" Effect="{StaticResource PremiumShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header / Title Bar -->
            <Border Grid.Row="0" Background="#FFFFFF" Padding="20,15" CornerRadius="11,11,0,0">
                <DockPanel>
                    <TextBlock Text="PointyPal Setup" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Border x:Name="SafeModeBanner" HorizontalAlignment="Right" Background="#FFF9C4" Padding="10,5" CornerRadius="4" Visibility="Collapsed">
                        <TextBlock Text="⚠️ Safe Mode Active" FontWeight="Bold" Foreground="#856404" VerticalAlignment="Center"/>
                    </Border>
                </DockPanel>
            </Border>

            <!-- Content Area -->
            <Grid Grid.Row="1" Margin="40,30">
                             <!-- Step 1: Welcome -->
                <StackPanel x:Name="StepWelcome" Visibility="Visible">
                    <TextBlock Style="{StaticResource Header1}" Text="Welcome to PointyPal"/>
                    <TextBlock Style="{StaticResource SubHeader}" Text="PointyPal is an AI assistant that sees your screen, listens to your voice, and points the way."/>
                    
                    <StackPanel Margin="0,20">
                        <BulletDecorator Margin="0,5">
                            <BulletDecorator.Bullet>
                                <Ellipse Fill="#0071E3" Width="8" Height="8" Margin="0,5,10,0"/>
                            </BulletDecorator.Bullet>
                            <TextBlock Text="Understands your screen context to help with any app." TextWrapping="Wrap"/>
                        </BulletDecorator>
                        <BulletDecorator Margin="0,5">
                            <BulletDecorator.Bullet>
                                <Ellipse Fill="#0071E3" Width="8" Height="8" Margin="0,5,10,0"/>
                            </BulletDecorator.Bullet>
                            <TextBlock Text="Listens to your voice commands (Hold Right Ctrl)." TextWrapping="Wrap"/>
                        </BulletDecorator>
                        <BulletDecorator Margin="0,5">
                            <BulletDecorator.Bullet>
                                <Ellipse Fill="#0071E3" Width="8" Height="8" Margin="0,5,10,0"/>
                            </BulletDecorator.Bullet>
                            <TextBlock Text="Points at apps and answers your questions directly." TextWrapping="Wrap"/>
                        </BulletDecorator>
                    </StackPanel>

                    <TextBlock Margin="0,20,0,0" Text="Real AI features require a Worker connection. This wizard will help you set it up." TextWrapping="Wrap" FontStyle="Italic"/>
                </StackPanel>

                <!-- Step 2: Privacy -->
                <StackPanel x:Name="StepPrivacy" Visibility="Collapsed">
                    <TextBlock Style="{StaticResource Header1}" Text="Privacy &amp; Data"/>
                    <TextBlock Style="{StaticResource SubHeader}" Text="Your privacy is our priority. PointyPal only captures data when you ask."/>

                    <GroupBox Header="Data Transmissions" Margin="0,0,0,15" Padding="10">
                        <StackPanel>
                            <TextBlock Text="• Screenshots are sent to Claude via your Worker." Margin="0,2"/>
                            <TextBlock Text="• Voice audio is sent to transcription via your Worker." Margin="0,2"/>
                            <TextBlock Text="• Response text is sent to voice output via your Worker." Margin="0,2"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Local Control" Margin="0,0,0,15" Padding="10">
                        <StackPanel>
                            <TextBlock Text="• Settings and logs are stored locally on this PC." Margin="0,2"/>
                            <TextBlock Text="• Interaction history stays on your machine." Margin="0,2"/>
                        </StackPanel>
                    </GroupBox>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                        <Button x:Name="ApplyPrivacyDefaultsBtn" Content="Apply Privacy-Safe Defaults (Recommended)" Style="{StaticResource PrimaryWizardButton}" Click="ApplyPrivacyDefaultsBtn_Click" Padding="20,10"/>
                        <Button x:Name="KeepCurrentPrivacyBtn" Content="Keep Current" Style="{StaticResource WizardButton}" Click="KeepCurrentPrivacyBtn_Click"/>
                    </StackPanel>
                </StackPanel>

                <!-- Step 3: Worker Connection -->
                <StackPanel x:Name="StepWorker" Visibility="Collapsed">
                    <TextBlock Style="{StaticResource Header1}" Text="Worker Connection"/>
                    <TextBlock Style="{StaticResource SubHeader}" Text="PointyPal needs a Worker to connect to AI services."/>

                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Worker URL:" VerticalAlignment="Center" Margin="0,0,15,10"/>
                        <TextBox x:Name="WorkerUrlBox" Grid.Column="1" Height="30" Margin="0,0,0,10" Padding="5,0" VerticalContentAlignment="Center"/>

                        <TextBlock Grid.Row="1" Text="Client Key:" VerticalAlignment="Center" Margin="0,0,15,10"/>
                        <PasswordBox x:Name="WorkerKeyBox" Grid.Row="1" Grid.Column="1" Height="30" Margin="0,0,0,10" VerticalContentAlignment="Center"/>
                    </Grid>

                    <TextBlock Margin="0,0,0,10" HorizontalAlignment="Right">
                        <Hyperlink Click="HelpLink_Click">How do I get a Worker?</Hyperlink>
                    </TextBlock>

                    <StackPanel x:Name="WorkerStatusPanel" Margin="0,10">
                        <TextBlock x:Name="WorkerStatusText" Text="Status: Not checked" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBlock x:Name="WorkerDetailsText" Text="" FontSize="11" Foreground="#86868B" TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                        <Button x:Name="CheckWorkerBtn" Content="Check Connection" Style="{StaticResource WizardButton}" Click="CheckWorkerBtn_Click"/>
                        <Button x:Name="SaveWorkerBtn" Content="Save Connection" Style="{StaticResource WizardButton}" Click="SaveWorkerBtn_Click"/>
                    </StackPanel>
                </StackPanel>

                <!-- Step 4: Voice Input -->
                <StackPanel x:Name="StepVoiceInput" Visibility="Collapsed">
                    <TextBlock Style="{StaticResource Header1}" Text="Voice Input"/>
                    <TextBlock Style="{StaticResource SubHeader}" Text="Test your microphone for voice commands."/>

                    <Border Background="#FFFFFF" Padding="20" CornerRadius="8" BorderBrush="#DDDDDD" BorderThickness="1">
                        <StackPanel>
                            <TextBlock x:Name="MicStatusText" Text="Detecting microphone..." FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <ProgressBar x:Name="MicLevelBar" Height="10" Margin="0,10" Maximum="100"/>
                            <TextBlock Text="Hold Right Ctrl to test microphone locally." HorizontalAlignment="Center" Foreground="#86868B"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
                        <Button x:Name="TestMicBtn" Content="Test Microphone" Style="{StaticResource WizardButton}" Click="TestMicBtn_Click"/>
                        <Button x:Name="TestRealSttBtn" Content="Test Real Transcription" Style="{StaticResource WizardButton}" Click="TestRealSttBtn_Click"/>
                    </StackPanel>
                    <TextBlock Text="* Real transcription sends a short audio sample to your Worker." FontSize="11" Foreground="#86868B" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Step 5: Voice Output -->
                <StackPanel x:Name="StepVoiceOutput" Visibility="Collapsed">
                    <TextBlock Style="{StaticResource Header1}" Text="Voice Output"/>
                    <TextBlock Style="{StaticResource SubHeader}" Text="PointyPal can speak responses using a digital voice (optional)."/>

                    <StackPanel Margin="0,10">
                        <CheckBox x:Name="EnableTtsCheck" Content="Enable Voice Output (TTS)" Margin="0,0,0,10" Checked="EnableTtsCheck_Checked" Unchecked="EnableTtsCheck_Unchecked"/>
                        <TextBlock Text="Requires ElevenLabs configured on your Worker." Foreground="#86868B" FontSize="12" Margin="25,0,0,20"/>
                    </StackPanel>

                    <Border Background="#FFFFFF" Padding="20" CornerRadius="8" BorderBrush="#DDDDDD" BorderThickness="1">
                        <StackPanel>
                            <TextBlock x:Name="TtsStatusText" Text="Voice output is currently disabled." FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <Button x:Name="TestTtsBtn" Content="Test Voice Output" Style="{StaticResource WizardButton}" Click="TestTtsBtn_Click" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="* Testing sends a short test phrase to your Worker." FontSize="11" Foreground="#86868B" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>

                <!-- Step 6: Hotkeys -->
                <StackPanel x:Name="StepHotkeys" Visibility="Collapsed">
                    <TextBlock Style="{StaticResource Header1}" Text="Daily Hotkeys"/>
                    <TextBlock Style="{StaticResource SubHeader}" Text="Learn the essential keys for PointyPal."/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Right Ctrl (Hold)" FontWeight="Bold" Foreground="#0071E3" Margin="0,0,20,15"/>
                        <TextBlock Grid.Column="1" Text="Ask PointyPal by voice. Release to send." Margin="0,0,0,15"/>

                        <TextBlock Grid.Row="1" Text="Ctrl + Space" FontWeight="Bold" Foreground="#0071E3" Margin="0,0,20,15"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="Quick Ask (Type your request)." Margin="0,0,0,15"/>

                        <TextBlock Grid.Row="2" Text="Escape" FontWeight="Bold" Foreground="#0071E3" Margin="0,0,20,15"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="Cancel active operation / hide bubble." Margin="0,0,0,15"/>

                        <TextBlock Grid.Row="3" Text="F9" FontWeight="Bold" Foreground="#0071E3" Margin="0,0,20,15"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="Show status and diagnostics." Margin="0,0,0,15"/>
                    </Grid>

                    <Button Content="Open full hotkey reference" Style="{StaticResource WizardButton}" Click="OpenHotkeysBtn_Click" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                </StackPanel>

                <!-- Step 7: Real Flow Test -->
                <StackPanel x:Name="StepRealFlow" Visibility="Collapsed">
                    <TextBlock Style="{StaticResource Header1}" Text="Ready to Go?"/>
                    <TextBlock Style="{StaticResource SubHeader}" Text="Optionally run a real end-to-end test."/>

                    <StackPanel HorizontalAlignment="Center" Margin="0,20">
                        <Button x:Name="RunRealTestBtn" Content="Run Quick Ask Test" Style="{StaticResource WizardButton}" Click="RunRealTestBtn_Click" Width="250"/>
                        <Button x:Name="RunScreenshotTestBtn" Content="Run Screen Context Test" Style="{StaticResource WizardButton}" Click="RunScreenshotTestBtn_Click" Width="250"/>
                        <Button x:Name="SkipRealTestBtn" Content="Skip Real Flow Test" Style="{StaticResource WizardButton}" Click="SkipRealTestBtn_Click" Width="250"/>
                    </StackPanel>

                    <Border x:Name="RealTestResultPanel" Background="#E8F2FF" Padding="15" CornerRadius="6" Margin="0,10" Visibility="Collapsed">
                        <TextBlock x:Name="RealTestResultText" Text="Test in progress..." Foreground="#004A99" FontWeight="Bold" TextAlignment="Center"/>
                    </Border>

                    <TextBlock Text="* Real tests use your configured Worker and may incur costs." FontSize="11" Foreground="#86868B" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>

                <!-- Step 8: Complete -->
                <StackPanel x:Name="StepComplete" Visibility="Collapsed">
                    <TextBlock Style="{StaticResource Header1}" Text="Setup Complete!"/>
                    <TextBlock Style="{StaticResource SubHeader}" Text="PointyPal is ready to assist you."/>

                    <Border Background="#FFFFFF" Padding="20" CornerRadius="8" BorderBrush="#DDDDDD" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="✓ Worker Configured" Margin="0,5"/>
                                <TextBlock x:Name="FinalVoiceInputText" Text="✓ Voice Input Ready" Margin="0,5"/>
                                <TextBlock x:Name="FinalVoiceOutputText" Text="✓ Voice Output Ready" Margin="0,5"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="✓ Privacy Settings Applied" Margin="0,5"/>
                                <TextBlock x:Name="FinalDevModeText" Text="- Developer Mode Disabled" Margin="0,5"/>
                                <CheckBox x:Name="StartWithWindowsCheck" Content="Start with Windows" Margin="0,5"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30">
                        <Button Content="Open Control Center" Style="{StaticResource WizardButton}" Click="OpenControlCenterBtn_Click"/>
                        <Button Content="Finish" Style="{StaticResource PrimaryWizardButton}" Click="FinishBtn_Click" Width="150"/>
                    </StackPanel>
                </StackPanel>

            </Grid>

            <!-- Navigation Footer -->
            <Border Grid.Row="2" Background="#FFFFFF" Padding="20,15" CornerRadius="0,0,11,11">
                <DockPanel>
                    <Button x:Name="PrevBtn" Content="Back" Style="{StaticResource WizardButton}" Click="PrevBtn_Click" HorizontalAlignment="Left" Visibility="Collapsed"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="SkipBtn" Content="Skip for Now" Style="{StaticResource WizardButton}" Click="SkipBtn_Click"/>
                        <Button x:Name="NextBtn" Content="Continue" Style="{StaticResource PrimaryWizardButton}" Click="NextBtn_Click"/>
                    </StackPanel>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</Window>
