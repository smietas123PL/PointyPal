<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Brushes.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Premium Card Style -->
    <Style x:Key="PremiumCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardGradientBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="Padding" Value="22"/>
        <Setter Property="Effect" Value="{StaticResource SubtleSurfaceShadow}"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>

    <Style x:Key="HeroCardStyle" TargetType="Border" BasedOn="{StaticResource PremiumCardStyle}">
        <Setter Property="Background" Value="{StaticResource HeroSurfaceBrush}"/>
        <Setter Property="Padding" Value="28"/>
        <Setter Property="CornerRadius" Value="30"/>
        <Setter Property="MinHeight" Value="188"/>
        <Setter Property="Margin" Value="0,0,0,18"/>
        <Setter Property="Effect" Value="{StaticResource PremiumCardShadow}"/>
    </Style>

    <Style x:Key="FeatureCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource FeatureCardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Margin" Value="7"/>
        <Setter Property="MinHeight" Value="98"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource AccentBorderStrongBrush}"/>
                <Setter Property="Background" Value="{StaticResource FeatureCardHoverBrush}"/>
                <Setter Property="Effect" Value="{StaticResource FeatureCardGlowEffect}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Stat Card Style -->
    <Style x:Key="StatCardStyle" TargetType="Border" BasedOn="{StaticResource FeatureCardStyle}">
        <Setter Property="MinHeight" Value="116"/>
    </Style>

    <Style x:Key="CompactFeatureCardStyle" TargetType="Border" BasedOn="{StaticResource FeatureCardStyle}">
        <Setter Property="Padding" Value="18"/>
        <Setter Property="MinHeight" Value="96"/>
    </Style>

    <!-- Settings Row Card Style -->
    <Style x:Key="SettingsRowCardStyle" TargetType="Border">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="20,14"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SidebarPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SidebarGradientBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Effect" Value="{StaticResource SubtleSurfaceShadow}"/>
    </Style>

    <Style x:Key="CommandBarStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CommandBarBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,16,0,0"/>
    </Style>

    <Style x:Key="FeatureIconTileStyle" TargetType="Border">
        <Setter Property="Width" Value="48"/>
        <Setter Property="Height" Value="48"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="Background" Value="{StaticResource IconTileGradientBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="0,0,18,0"/>
    </Style>

    <Style x:Key="PageContentStyle" TargetType="Grid">
        <Setter Property="MaxWidth" Value="1080"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Width" Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}"/>
    </Style>

    <Style x:Key="PageSectionTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryAccentBrush}"/>
        <Setter Property="Opacity" Value="0.92"/>
        <Setter Property="Margin" Value="8,2,0,12"/>
    </Style>

    <Style x:Key="PageLeadStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSecondarySoftBrush}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="LineHeight" Value="20"/>
        <Setter Property="Margin" Value="0,0,0,18"/>
    </Style>

</ResourceDictionary>
